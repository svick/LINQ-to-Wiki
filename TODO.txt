* XML documentation of generated types (e.g. Wiki)
* web application
* optionally turn off getting namespaces every time (or maybe cache them?)
* figure out what to do with tokens
* figure out what to do with warnings (maybe WarningsAsExceptions switch)
* consider using multipart/formdata when necessary
* Take()?
* all configuration in one place
* hadle != in Where() correctly
